/**
 * Zustand store barrel export.
 *
 * Each slice of state lives in its own file.
 * Add stores here as phases progress:
 *
 * Phase 2 → useAuthStore   (user session, preferences)
 * Phase 3 → useProblemStore (current problem, filters)
 * Phase 4 → useEditorStore  (code, language, solution status)
 * Phase 5 → useVisualizerStore (steps, playback state)
 */

// Placeholder — will be populated in Phase 2
export {};